<ul>
    <% posts.forEach ( post => { %>
        <li class="post-card <%- post.avatar %>">
            <p><%= post.content %></p>
            <img src="<%- 'images/' + post.avatar + '.png' %>">
        </li>
    <% }); %>
</ul>